#!/usr/bin/env sh
ws=$(i3-msg -t get_workspaces | jq '.[] | select(.focused==true).name' | cut -d"\"" -f2)
echo $ws
if [ $ws == 10 ]; then
    i3-msg workspace back_and_forth
else
    i3-msg workspace 10
fi


