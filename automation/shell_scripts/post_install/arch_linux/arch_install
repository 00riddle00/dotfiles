1)partition hdd


	cfdisk  /dev/sda

	delete partitions

	new-primary-512MB-enter
	end
	type - 82(swap)

	new-primary
	-make bootabble

	write-yes
	quit
	

	mkfs.ext4 /dev/sda2

	mount /dev/sda2 /mnt

	mkswap /dev/sda1

	swapon /dev/sda1


2) Installing the sys

	inet connect + ping test

	pacstrap /mnt base base-devel

	(20min, etc)

3) Configuring installation


	arch-chroot /mnt

	passwd(return)

	vi /etc/locale.gen  -US english, rm hashtags

	locale-gen


	cd /usr/share/zoneinfo
	ls
	cd /usr/share/zoneinfo/Europe/...

	ln -s /usr/share/zoneinfo/Europe/...   /etc/localtime

	echo [hostname] > /etc/hostname

	
	pacman -S grub-bios

	grub-install /dev/sda


	mkinitcpio -p linux

	grub-mkconfig -o /boot/grub/grub.cfg

	exit (arch-chroot)

	genfstab /mnt >> /mnt/etc/fstab

	umount /mnt

	reboot

